// Order model
export class Order {
  constructor(
    id,
    user_id,
    completed,
    cancelled,
    kitchen_cancelled,
    kitchen_accepted,
    kitchen_dispatched,
    kitchen_dispatched_time,
    completed_time,
    rider_id,
    kitchen_prepared,
    rider_assigned,
    paid,
    order_code,
    calculated_order_id,
    created_at,
    updated_at,
  ) {
    // fill the properties based on the constructor arguments
  }
}
