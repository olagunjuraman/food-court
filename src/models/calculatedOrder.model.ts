export class CalculatedOrder {
  constructor(
    id,
    order_id,
    meals,
    total_amount,
    free_delivery,
    delivery_fee,
    service_charge,
    address_details,
  ) {
    // fill the properties based on the constructor arguments
  }
}
